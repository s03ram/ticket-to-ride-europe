# ticket-to-ride-europe

Development
-----------

To enable the commit checker locally (recommended):

```bash
python -m pip install -e .[dev]
python -m pip install --upgrade pre-commit
pre-commit install
pre-commit run --all-files
```
